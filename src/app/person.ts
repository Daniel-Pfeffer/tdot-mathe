export class Person {
    name: string;
    mail: string;
    guess: number;
    gummy: number;
    mAndMs: number;

    constructor(name: string, mail: string, guess: number, gummy: number, mAndMs: number) {
        this.name = name;
        this.mail = mail;
        this.guess = guess;
        this.gummy = gummy;
        this.mAndMs = mAndMs;
    }
}
